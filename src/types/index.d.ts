export interface SliderImage {
  id: number;
  imageUrl: string;
  title: string;
  subtitle: string;
}

export interface NewsItem {
  id: number;
  imageUrl: string;
  event: string;
  title: string;
  description: string;
}
